<h1>Etiquetas</h1>

<ul>
  <% tags.forEach((tag) => { %>
    <li>Nombre de la etiqueta: <%= tag.name %></li> 
  <% }) %>
  <% if (tags.length == 0) { %>
    <h3>Aún no se han creado etiquetas</h3>
  <% } %>
</ul>
