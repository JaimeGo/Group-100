<h1>Etiquetas</h1>

<ul>
  <% tags.forEach((tag) => { %>
    <a href="<%=tagPathBuilder(tag)%>"><%=tag.name%></a> cuyo id es <%= tag.id %>
    <ul>
      
    </ul>
  <% }) %>
  <% if (tags.length == 0) { %>
    <h3>Aún no se han creado etiquetas</h3>
  <% } %>
</ul>
