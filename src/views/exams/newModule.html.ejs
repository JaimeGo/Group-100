<h1>Nuevo Módulo</h1>


<div>
  <form action="<%= submitModulePath %>" method="post">
    <% if (!mod.isNewRecord) { %>
      <input type="hidden" name="_method" value="post" />
    <% } %>
    <div>
      <label for="name">Nombre del módulo</label>
      <input type="text" name="name" value="<%= mod.name %>" /></input>
    </div>
    
    <br>
    <% if (!mod.isNewRecord) { %>
      <div>
        <input type="submit" name="update" value="Modificar Módulo" />
      </div>
    <% } else {%>
      <div>
        <input type="submit" name="create" value="Crear Módulo" />
      </div>
    <% } %>
  </form>
</div>