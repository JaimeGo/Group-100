<h1>Preguntas de <%= user.name %></h1>

<ul>
  <% questions.forEach((question) => { %>
    <li><h3><a href="<%=questionPathBuilder(question)%>"><%=question.title%></a></h3></li>
    <p><%=question.body%></p>
  <% }) %>
</ul>